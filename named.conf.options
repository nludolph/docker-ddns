options {
        directory "/var/cache/bind";
        dnssec-validation auto;
        recursion no;
        allow-transfer { none; };
        auth-nxdomain no;
        interface-interval 0;
        listen-on-v6 { none; };
        listen-on { any; };
};